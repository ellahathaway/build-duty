# Example Configs

Example `.build-duty.yml` configurations for different build-duty roles on the .NET SDK team.

Each file targets a different monitoring scope — copy one as a starting point and customize it for your rotation. Pass it to any command with `--config <path>`, or place it in your repo root as `.build-duty.yml`.

See the [main README](../README.md) for full configuration documentation.
